import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bad_camera_android',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
